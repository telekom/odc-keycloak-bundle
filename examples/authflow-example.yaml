apiVersion: keycloak.opendefense.cloud/v1alpha1
kind: AuthFlow
metadata:
  name: tenant-a-browser-mfa
  namespace: keycloak-dev  # Must match the Keycloak instance namespace
spec:
  realmRef: tenant-a
  alias: tenant-a-browser-mfa
  description: "Tenant A browser flow with MFA requirement"
  topLevel: true
  requireMFA: true
