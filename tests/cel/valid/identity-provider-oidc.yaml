apiVersion: keycloak.opendefense.cloud/v1alpha1
kind: IdentityProvider
metadata:
  name: cel-test-oidc-valid
spec:
  realmRef: master
  alias: cel-test-oidc-valid
  type: oidc
  enabled: true
  config:
    issuerUrl: https://idp.example.com
    clientId: cel-test-client
