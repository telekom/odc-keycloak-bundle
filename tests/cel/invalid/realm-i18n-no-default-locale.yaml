apiVersion: keycloak.opendefense.cloud/v1alpha1
kind: Realm
metadata:
  name: cel-test-i18n-no-default
spec:
  realmName: cel-test-i18n-no-default
  internationalizationEnabled: true
  supportedLocales:
    - en
    - de
  # defaultLocale is absent — CEL rule requires it when internationalizationEnabled is true
